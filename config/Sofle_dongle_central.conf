### SPLIT / DONGLE CONFIGURATION
CONFIG_ZMK_SPLIT_BLE_CENTRAL_PERIPHERALS=2

### LIGHTING CONFIGURATION
# The dongle has no LED strip of its own (no zmk,underglow chosen node).
# ZMK's multi-shield build (this shield + prospector_adapter) picks up
# config/Sofle.conf's CONFIG_ZMK_RGB_UNDERGLOW=y as a side effect of a
# shield-name fallback quirk in ZMK v0.3's CMake config-file resolution;
# without this explicit override the build fails with
# "A zmk,underglow chosen node must be declared".
CONFIG_ZMK_RGB_UNDERGLOW=n

### ZMK STUDIO CONFIGURATION
CONFIG_ZMK_STUDIO=y
CONFIG_ZMK_STUDIO_LOCKING=n
CONFIG_ZMK_STUDIO_LOCK_ON_DISCONNECT=n

### POINTING DEVICE CONFIGURATION
CONFIG_ZMK_POINTING=y

### BLUETOOTH CONFIGURATION
CONFIG_BT_CTLR_TX_PWR_PLUS_8=y
CONFIG_ZMK_BLE_EXPERIMENTAL_CONN=y
CONFIG_ZMK_BLE_EXPERIMENTAL_FEATURES=y
CONFIG_BT_PERIPHERAL_PREF_MIN_INT=6
CONFIG_BT_PERIPHERAL_PREF_MAX_INT=6
CONFIG_BT_PERIPHERAL_PREF_LATENCY=0
CONFIG_ZMK_SPLIT_BLE_PREF_INT=6
CONFIG_ZMK_SPLIT_BLE_PREF_LATENCY=0
CONFIG_ZMK_KEYBOARD_NAME="PandaKB_Sofle"
CONFIG_BT_GATT_ENFORCE_SUBSCRIPTION=n

### PROSPECTOR CONFIGURATION
CONFIG_PROSPECTOR_USE_AMBIENT_LIGHT_SENSOR=n
CONFIG_PROSPECTOR_FIXED_BRIGHTNESS=50
